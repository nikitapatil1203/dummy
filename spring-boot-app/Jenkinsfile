pipeline {
  agent any 
    stages {
      stage(build) {
        steps {
          sh 'cd spring-boot-app && mvn clean compile && mvn clean package'
        }






        
      }
    }
  


  
}
